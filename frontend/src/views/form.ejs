<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>File Upload Status</title>
    <style nonce="<%= nonce %>">
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
            padding: 0;
        }
        h1 {
            color: #333;
        }
        p {
            color: #555;
        }
        a {
            color: #007bff;
            text-decoration: none;
        }
        a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <h1>File Upload Status</h1>
    <% if (message) { %>
        <p><%= message %></p>
    <% } else { %>
        <p>No status message available.</p>
    <% } %>
    <a href="/upload">Go back to the upload page</a>
</body>
</html>
